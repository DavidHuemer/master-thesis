data/code/arithmetic/StringIndexOf.java
----------
```java
//@ requires s != null;
//@ ensures (\exists int i; 0 <= i && i < s.length(); s.charAt(i) == sub) ==> \result == (\min int j; 0 <= j && j < s.length() && s.charAt(j) == sub; j);
//@ ensures !(\exists int i; 0 <= i && i < s.length(); s.charAt(i) == sub) ==> \result == -1;
```