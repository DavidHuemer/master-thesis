data/code/arithmetic/Pow.java
----------
```java
//@ requires a > 0 && b >= 0;
//@ requires a <= 10 && b <= 10;
//@ ensures \result == (b == 0 ? 1 : (\product int i; 0 <= i && i < b; a));
//@ signals (IllegalArgumentException e) a <= 0 || b < 0 || a > 10 || b > 10;
```